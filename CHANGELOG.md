# سجل التغييرات (Changelog)

جميع التغييرات الملحوظة في هذا المشروع سيتم توثيقها في هذا الملف.

## [1.0.0-PROD] - 2026-09-14 — الإغلاق النهائي الصارم للمسار الأول (Track 1 Zero-Defect Hardening & Sign-Off)

### 💎 التثبيت الصارم وإغلاق المسار الأول بالكامل (100% Zero-Defect Hardened)
- **المرحلة 1: مسار البلاطات والـ 4K ورندر Finalize الذري (Tile Pipeline & Atomic Finalize)**
  - **حماية انقطاع الشبكة والتراجع الذري**: معالجة انقطاع الاتصال المفاجئ أثناء تحميل بلاطات 4K عبر تراجع ذري آمن (`_rollbackTransaction` / `composition.rollback`) ومسح الملفات المؤقتة فوراً دون ترك أي رواسب تالفة.
  - **تفريغ فوري لذاكرة الـ GPU والـ RAM**: تصفير أبعاد الـ Canvas فور انتهاء التجميع (`width=0; height=0;`) وإغلاق Bitmaps ونقل البيانات عبر Transferable ArrayBuffer لمنع تسريب الذاكرة تحت أحمال 15,000+ بلاطة.
  - **تدفق تقدم سلس**: حظر تجمد واجهة المستخدم بتحديث سرعة التنزيل بالبلاطات/ثانية وعرض التقدم عبر `requestAnimationFrame`.
  - **موازنة البلاطات المسبقة**: حظر التنزيل المفرط بنظام تنبيه تفاعلي عند 2,500 بلاطة وحد صارم (Hard Limit) عند 20,000 إلى 25,000 بلاطة.

- **المرحلة 2: محرك التحريك وتزامن الكاميرا في After Effects (Motion & Timeline Sync)**
  - **دعم كافة المقاسات والنسب**: حسابات رياضية دقيقة لنسب العرض للشاشات العريضة (21:9)، العمودية (9:16)، المربعة (1:1)، و(4:3) وتطابق الكادر بالبكسل.
  - **تأمين وضع التسجيل (Record Mode)**: فصل وضع التسجيل تلقائياً عند الانتقال لكومبوزيشن غير مرتبط بـ OpenGeo لحماية مشاريع المستخدم.
  - **تفريغ الكي فريمات الذري**: مسح الكي فريمات داخل `OpenGeo: Clear Camera Keyframes` ذرية واحدة مع تثبيت الكاميرا في الزمن الحالي بـ `valueAtTime`.
  - **معالجة خط التاريخ الدولي (Antimeridian)**: حساب المسار الأقصر الدائري في `MercatorProjection.cameraPixelDistance` و `normalizeLng` لمنع الدوران العكسي 360 درجة.

- **المرحلة 3: نظام البن المكاني ومثبتات الـ Null (Spatial Pin Hardening)**
  - **تثبيت ميكروسكوبي Sub-Pixel**: دقة التثبيت الجغرافي بالبكسل من زووم Z=2 إلى Z=18 بنسبة خطأ أقل من 1e-9 بكسل.
  - **دعم كامل للغة العربية والرموز العالمية**: جسر UTF-16 مخصص يسمح بإنشاء طبقات Text وNull بأسماء عربية صريحة دون أي تشويه.
  - **حصانة الطبقات اليتيمة (Orphan Pin Recovery)**: حماية تعبيرات After Effects بشرط أمان `if (!ctrl) { value; }` لمنع كسر التايملاين عند حذف الكنترولر يدوياً.

- **المرحلة 4: الطبقات والمتجهات والـ GeoJSON (Vectors & Layers Management)**
  - **حارس سعة الـ GeoJSON**: فحص صارم يرفض الحزم التي تتجاوز 10MB، أو 5,000 ميزة، أو 100,000 نقطة لحماية After Effects من التجمد، مع التحقق من إغلاق الحلقات ونطاق الإحداثيات.
  - **التراجع الذري لمعاملات الطبقات**: بناء المتجهات وحذفها والتحكم برؤيتها داخل `withUndoGroup` ذرية واحدة قابلة للتراجع بنقرة `Ctrl + Z`.
  - **لوحة طبقات آمنة**: إدارة الطبقات عبر الـ DOM النقي بدون أي ثغرات `innerHTML` (Zero Sinks) ومزامنة حية مع After Effects.

- **المرحلة 5: واجهة المستخدم والـ HUD والشاشات الضيقة (UI/UX & Narrow Docking)**
  - **التكيف مع اللوحات الضيقة (280px - 420px)**: قواعد `@media` تضغط أزرار التحكم والشريط السفلي إلى أيقونات صافية مع الحفاظ على التلميحات والأزرار الرئيسية مقروءة بنسبة 100%.
  - **كاش وDebounce شارة الموقع**: محاذاة 500ms ومطابقة جغرافية محلية في الذاكرة وكاش LRU بحد أقصى 200 عنصر لمنع استنزاف طلبات الشبكة أثناء السحب السريع.
  - **مقياس مسافات وبوصلة زجاجية عالية التباين**: مقياس مسافات جيوديسي متدرج بخلفية Glassmorphism وظلال نصوص تضمن وضوح القراءة فوق الخرائط الفاتحة والداكنة والأقمار الصناعية.

- **المرحلة 6: مدير المشاريع وتوليد صور المعاينة (Project Maps & Thumbnails)**
  - **مرونة المشاريع غير المحفوظة**: دعم العمل على مشاريع `Untitled.aep` وإظهار تنبيه توجيهي أنيق بدون أي رسائل خطأ مسار مفقود.
  - **استكشاف المشاريع الضخمة**: فهرسة واستعراض خرائط المشاريع الكبيرة في أقل من 50ms مع ترتيب الكومبوزيشن النشط أولاً وسقف حماية 200 خريطة.
  - **قص المعاينة بدقة الأبعاد الأصلية**: استخراج وحفظ صور الـ Thumbnail بنسبة الكادر الحقيقية (16:9، 9:16، 1:1) مع خلفية ضبابية تملأ الهوامش دون أي تشويه أو تمدد.

- **المرحلة 7: بوابة الاعتماد النهائي (Final V1 Sign-Off)**
  - **تشغيل شامل للمختبر الآلي الموحد**: اجتياز 6 مجموعات اختبارات تلقائية بنجاح 100% تشمل اختبار محاكاة الإنتاج الواقعي لمدة 30 دقيقة.
  - **التحقق الأمني الشامل**: اجتياز `npm run verify:release` بدون أي تحذيرات أو ثغرات CSP أو DOM Sinks.
  - **الإغلاق الرسمي**: إغلاق المسار الأول رسمياً والانتقال الكامل والآمن لتطوير خصائص المسار الثاني (Track 2).

## [1.0.0] - 2026-08-10

### 🚀 الميزات الجديدة (Features)
- **MapTiler Integration**: تم دمج مزود MapTiler رسمياً كمصدر أساسي لصور الأقمار الصناعية عالية الدقة. يمكن للمستخدم إدخال مفتاح الـ API الخاص به من لوحة التحكم لاستخدامه.
- **التصميم المعماري الجديد**: تم الاستقرار على محرك `OpenGeoEngine.js` المبني على JavaScript النقي وتمت إزالة تعقيدات TypeScript و Rollup غير المستخدمة لتسريع الأداء وتسهيل الصيانة.

### 🛡️ الأمن والحماية (Security)
- **إزالة `eval` (OG-004)**: تم استبدال جميع استدعاءات `eval()` الخطيرة في المضيف (Host) بـ `JSON.parse` الآمن مع معالجة الأخطاء.
- **تأمين جسر التواصل (OG-003)**: تم برمجة نظام لتغليف المتغيرات بواسطة `JSON.stringify` بدلاً من الدمج النصي (String Interpolation) لحماية الإضافة من ثغرات حقن الكود (Code Injection) في `AEBridge.js` و `SpatialPin.js`.
- **التنظيف العميق للديون البرمجية (Deep Audit Fixes)**: أثناء الفحص الشامل الإضافي، تم اكتشاف وإصلاح ثغرات حقن نصي (String Injection) مشابهة كانت مختبئة في `app.js`، `MetadataManager.js`، `AESyncEngine.js`، و `FinalizeController.js`.
- **تنظيف بيئة الإنتاج (OG-006)**: تم تعديل `manifest.xml` لحذف صلاحيات القراءة الموسعة `--allow-file-access-from-files` و `--mixed-context`، واستبعاد مجلدات التطوير وملف `.debug` الخطير من إصدار الـ Release.
- **تأمين الـ API (OG-005)**: تم فرض وجود مفتاح API لمزود Stadia Terrain لمنع تسريب بيانات أو حدوث أخطاء 401.

### 🐛 إصلاحات الأخطاء (Bug Fixes)
- **تصحيح الحواف السوداء (TileGrid)**: إضافة هامش أمان للمعادلة الرياضية `getCompVisibleTiles` لمنع ظهور فراغات سوداء عند حدود الشاشة.
- **إصلاح الخطوط الفاصلة الدقيقة (Hairline Seams)**: إزالة نسبة التداخل (`Overlap`) وتفعيل `BlendingMode.ALPHA_ADD` للبلاطات في `compBuilder.jsx` للحصول على خريطة متصلة بـ 0 بكسل تداخل وألوان نقية.
- **إصلاح الحفظ التلقائي (OG-002)**: تم إصلاح خطأ `_queueAutoExport` بتوجيهه للوحدة المسؤولة عنه `syncManager`.
- **ديناميكية المسارات (OG-009)**: استبدال مسار التثبيت الصلب `C:/Program Files` بمسارات تعتمد ديناميكياً على بيئة عمل Adobe (`SystemPath.USER_DATA` و `EXTENSION`) لحل مشاكل الصلاحيات للمستخدمين الذين يثبتون الإضافة في مسارات مختلفة.

### 🛠️ نظام البناء والاختبار (Build & Test)
- **مسار النسخ الذكي (OG-001)**: إعادة برمجة `build.js` بالكامل ليصبح نظام ذكي يعزل ملفات التطوير `tests, src, node_modules` وينسخ فقط ما يلزم لبيئة الإنتاج.
- **التحقق الآلي (OG-001)**: إضافة `verify-package.js` للتحقق من سلامة كافة الروابط في `index.html` قبل الموافقة على الإصدار.
- **المزامنة (OG-012)**: إضافة سكريبت `sync-versions.js` لمزامنة رقم الإصدار في `manifest.xml` مع `package.json` أوتوماتيكياً.
- **اختبارات الدخان (OG-010)**: توفير `test.js` للتأكد من سلامة المعالجة الأمنية قبل بناء الإضافة.
