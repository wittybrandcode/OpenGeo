# دستور وقوانين الحوكمة الهندسية لمشروع OpenGeo (AGENTS.md)
# OpenGeo Core Architecture Constitution & Governance Rules

> **الهدف الاستراتيجي الأسمى:**  
> بناء وتطوير محرك خرائط مفتوح المصدر لبرنامج Adobe After Effects يتفوق كلياً على التطبيقات التجارية الاحتكارية (وعلى رأسها **GEOlayers 3**) في السرعة، واستقرار الذاكرة العشوائية، ودقة الـ 4K/8K، وسلاسة التحريك، وتكامل البيانات الجغرافية الحقيقية مع بيئة الموشن جرافيكس، مع الحفاظ الصارم على موثوقية النظام بنسبة **0 خطأ**.

---

## 🏛️ المبادئ الدستورية غير القابلة للكسر (Non-Negotiable Laws)

### 1. قانون الصرامة والدقة (Strict Precision & Zero-Regressions)
* يُمنع التعديل العشوائي أو التخمين البرمجي. أي تعديل في الكود يجب أن يكون مبنياً على تحليل مسبق وفهم دقيق لآثاره الجانبية (Side Effects).
* **بروتوكول الاستثناءات والتطوير:** لا يجوز تعديل القواعد الهندسية الراسخة إلا وفق **دراسة معمقة مثبتة بالأدلة** تثبت أن المسار الجديد أدق هندسياً، أو أنفع للأداء، أو يحل تعارضاً بنيوياً، مع تحديث الدستور فور اعتماد المسار الأفضل.

### 2. قانون النجاح الشامل للاختبارات (The 100% Green Law)
* لا يُعتبر أي تعديل برمجياً مكتملاً ما لم تجتز **كافة حزم الاختبارات الأربع بنسبة 100% نجاح كامل**:
  ```bash
  node scripts/run-all-tests.js
  ```
  (تشمل: اختبارات الانحدار والأمان، مسار بلاطات 4K وحقن الأعطال، إعادة تحجيم الـ Viewport، وبنية المتجهات والـ Expressions).
* يجب دائماً التأكد من نجاح التحقق الأمني وبوابة الإصدار:
  ```bash
  npm run verify:release
  ```

### 3. دستور أبعاد الكومبوزيشن ودقة الـ 4K (4K/UHD Integrity)
* يُمنع منعاً باتاً افتراض أبعاد ثابتة (مثل 1920×1080) في أي مرحلة من مراحل المعاينة (Preview)، أو مسار الحركة (Trajectory)، أو التثبيت النهائي (Finalize).
* يجب دائماً احترام أبعاد الكومبوزيشن الحقيقية عبر:
  `app.session.composition` في الواجهة، و `opengeoFindDocumentOuterComp / opengeoFindDocumentMapComp` في After Effects.
* يجب أن يظل مركز دوران الخريطة `MapPivot` وطبقة الكنترولر متمركزين بدقة في نصف أبعاد الكومبوزيشن الحقيقية `[compWidth / 2, compHeight / 2]`.

### 4. قانون نظافة الذاكرة وتصفير أسطح الـ GPU (Zero VRAM Leaks)
* معالجة البلاطات بدقة 4K تستهلك عشرات الميجابايتات من الذاكرة الرسومية.
* أي كائن `OffscreenCanvas` أو `Canvas` أو `ImageBitmap` يتم استخدامه في Worker أو Main Thread **يجب إغلاقه وتصفير أبعاده فورياً**:
  ```javascript
  canvas.width = 0;
  canvas.height = 0;
  canvas = null;
  bitmap.close();
  ```
* فك تشفير البلاطات يجب أن يتم على دفعات مجزأة (`batchSize = 16`) لمنع قفزات استهلاك الذاكرة (Memory Spikes).

### 5. قانون المعاملات الذرية في After Effects (Atomic Undo Groups)
* كل تعديل أو كتابة تطال مشروع After Effects من جهة ExtendScript يجب أن تُنفذ بالكامل داخل دالة `withUndoGroup(name, operation)`.
* الهدف: ضمان أن ضغطة زر واحدة على `Ctrl+Z` من قبل المستخدم تعيد المشروع لحالته السابقة فوراً ودون ترك طبقات يتيمة أو ملفات معلقة.

### 6. قانون الفصل الصارم بين الهويات الجغرافية (Identity Contract)
* يُمنع الخلط بين هوية التنزيل والشبكة (`downloadKey`) وهوية الموضع على الكوكب (`placementKey`).
* عند زوايا الرؤية الواسعة للكرة الأرضية (Low Zoom)، يتم تنزيل المورد مرة واحدة فقط وربطه بكافة مواضع العالم المكررة (Wrapped Placements) دون أي هدر في حركة البيانات أو الذاكرة.

### 7. التوافق العابر للإصدارات (Backward & CEP Compatibility)
* يجب الحفاظ على التوافق التام مع محركات After Effects القديمة والحديثة (من CC 2018 حتى أحدث إصدارات 2026).
* دعم بدائل CEP الاحتياطية (Polyfills & Fallbacks) لأي ميزة حديثة (مثل `Worker`، `OffscreenCanvas`، `ResizeObserver`).

---

## 🎯 معايير التفوق على GEOlayers 3
عند تصميم أو تطوير أي ميزة جديدة، يجب مراعاة المعايير التنافسية التالية:
1. **السرعة اللحظية:** تجميع البلاطات وعرضها في After Effects في أجزاء من الثانية بفضل الـ Worker والـ Single-Flight Caching.
2. **المرونة البصرية:** دعم كامل للأنماط المتجهية (Vector Outlines)، وتخصيص الدبابيس (Spatial Pins)، والمؤشرات ثلاثية الأبعاد (3D Tracking Nulls).
3. **الاستقلالية والمصادر المفتوحة:** دعم كامل لمزودي الخرائط العالميين المفتوحين والمجانيين (OSM, Carto, Stamen, Esri, Custom XYZ) وتوفير بنية شفافة وسهلة التخصيص للمجتمع.
